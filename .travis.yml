language: java
sudo: false
install: true

jdk:
  - oraclejdk8

script:
  - mvn clean install -Dmaven.compiler.source=1.6 -Dmaven.compiler.target=1.6 -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=warn

after_success:
  - bash <(curl -s https://codecov.io/bash)
